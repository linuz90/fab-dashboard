interface:
  display_name: "Share Card"
  short_description: "Share card and connector blueprints safely"
  default_prompt: "Use $share-card to prepare a privacy-safe blueprint of a card, its main blocks, and connector shapes."
